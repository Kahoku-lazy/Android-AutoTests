---
name: doc-mgmt-devtest-module-triad
description: 05-开发与测试按平台模块子目录组织：编程工作流/代码质量/质量与验收手册
type: project
---

`dev_docs/05-开发与测试/`（2026-07-10 更新）：

1. **按平台功能模块建子目录**（例外于主目录平铺）：`仪表盘/` `设备管理/` `元素定位/` `用例管理/` `执行引擎/` `测试报告/` `AI助手/` `平台级/`。
2. **每模块固定三份核心 HTML**（文件名不再带模块前缀）：
   - `编程工作流.html`
   - `代码质量分析.html`（现网盘点，禁止只 copy）
   - `质量与验收手册.html`（Review · 质量流程 · 报错诊断 · AC）
3. **测试方案 HTML 暂缓**；既有 `测试方案-*.md` 留在模块目录。仅仪表盘保留已跑过的 `测试报告.html`。
4. 平台横切进 `平台级/`；执行引擎专项诊断/截图同目录。
5. README 矩阵 + `index.html` Stage 5 路径均指向子目录。
