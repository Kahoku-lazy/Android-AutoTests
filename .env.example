# Android-AutoTests 环境变量配置
# 复制此文件为 .env 并根据实际情况修改
# cp .env.example .env

# Django
DJANGO_SECRET_KEY=change-me-to-a-random-secret-key-at-least-50-chars
DJANGO_DEBUG=True

# 设备配置
DEVICE_SERIAL=your-device-serial
SCREENSHOT_INTERVAL=0.5
SERVER_PORT=8766

# 数据库 (sqlite 无需安装 MySQL；mysql 需配置下方参数)
DB_ENGINE=sqlite
DB_NAME=android_autotests
DB_USER=root
DB_PASSWORD=your-database-password
DB_HOST=127.0.0.1
DB_PORT=3306

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# AgentScope — AI 引擎端口
AGENTSCOPE_PORT=8088

# JWT
JWT_ACCESS_TTL=3600
JWT_REFRESH_TTL=604800

# Admin（登录用户名密码在 Django DB 中，非环境变量）
# 首次运行通过 python manage.py createsuperuser 创建
