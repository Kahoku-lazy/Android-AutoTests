# Android-AutoTests 当前实现架构方案
<!-- DEPRECATED: 2026-07-15 内容已合并到 项目架构.md。本文档保留作为历史参考，不再维护。 -->
<!-- 请以 项目架构.md 为唯一真相来源。 -->

> 2026-07-01 · 基于实际代码生成（非计划文档）

---

## 一、设计视角：技术大框架

### 1.1 核心架构图

```mermaid
graph TB
    User[用户]

    subgraph Frontend["Vue 3 前端 :5173"]
        DP[设备管理]
        EL[元素定位]
        EM[元素管理]
        CM[测试用例]
        TR[执行引擎]
        RG[测试报告]
        AI_Vue[AI 助手]
    end

    subgraph Django["Django 后端 :8765 Daphne"]
        GW["gateway/ JWT中间件 WS路由"]
        subgraph Apps["apps/ 6个业务模块"]
            EL2["element_locator 12端点"]
            DP2["device_pool 12端点"]
            CM2["case_manager 5端点"]
            TR2["test_runner 5端点"]
            RG2["report_generator 3端点"]
            AI2["ai_assistant 18端点"]
        end
        SH["shared/auth/ JWT签发验证"]
    end

    subgraph AS["AgentScope AI :8000 FastAPI"]
        Tools["14 自定义工具"]
        Team["5 子智能体模板"]
        RAG["ChromaDB 32篇"]
    end

    subgraph Data["数据层"]
        SQLite[("SQLite/MySQL")]
        Redis[("Redis")]
    end

    subgraph Device["设备控制"]
        U2[uiautomator2]
        ADB[ADB]
        Phone[Android]
    end

    User --> Frontend
    Frontend -->|"/api/* JWT"| Django
    Frontend -->|"/agentscope/* SSE"| AS
    GW --> Apps
    Django --> SQLite
    AS --> Redis
    Django --> Redis
    AS -->|"Tool调用 Django ORM"| Apps
    Django --> U2 --> ADB --> Phone
```

### 1.2 两条执行通道

```mermaid
flowchart LR
    User["用户"]

    subgraph ChannelA["通道 A — 手动操作"]
        A1["Vue UI 表单按钮"]
        A2["HTTP /api/*"]
        A3["Django View"]
        A4["Django ORM"]
        A5["uiautomator2"]
        A6["Android 设备"]
        A1 --> A2 --> A3 --> A4 --> A5 --> A6
    end

    subgraph ChannelB["通道 B — AI 智能"]
        B1["ChatView 输入框"]
        B2["SSE 流式请求"]
        B3["AgentScope Agent"]
        B4["ReAct 推理循环"]
        B5["自定义 Tool"]
        B6["Django ORM"]
        B7["Android 设备"]
        B1 --> B2 --> B3 --> B4 --> B5 --> B6 --> B7
    end

    User --> A1
    User --> B1
```

### 1.3 分层职责

```mermaid
flowchart TB
    L6["AI 引擎层<br/>Agent: ReAct 推理 | Toolkit: 14工具 | Agent Team: 5模板 | RAG: ChromaDB | 调度: APScheduler"]
    L5["设备控制层<br/>uiautomator2 → ADB → Android | DevicePool 单例"]
    L4["数据层<br/>Django ORM → SQLite(dev)/MySQL(prod) | Redis(AgentScope)"]
    L3["业务层 apps/<br/>5 v1: Django View+JsonResponse | ai_assistant: AgentScope 桥接"]
    L2["网关层 gateway/<br/>JWT中间件 | WebSocket路由 | 公共白名单"]
    L1["表示层 Vue 3<br/>8个模块 | Element Plus + 动森主题 | Pinia + Vue Router + Axios | SSE 流式"]

    L1 --> L2 --> L3 --> L4 --> L5 --> L6
    style L1 fill:#8B7355,color:#fff
    style L2 fill:#A0896C,color:#fff
    style L3 fill:#C4B5A0,color:#333
    style L4 fill:#DFD5C7,color:#333
    style L5 fill:#6B9E6B,color:#fff
    style L6 fill:#4A3A28,color:#fff
```

### 1.4 设计模式

```mermaid
graph LR
    subgraph P1["三层架构"]
        V[Vue 表示]
        D[Django 业务]
        A[AgentScope AI]
    end

    subgraph P2["1:1:1 映射"]
        V1[Vue模块]
        D1[Django App]
        T1[AgentScope Tool]
    end

    subgraph P3["三道防火墙"]
        F1["①service不跨App导入"]
        F2["②读ORM直通/写走api"]
        F3["③外部仅HTTP API"]
    end

    subgraph P4["8种核心模式"]
        M1[Tool模式: ToolBase子类]
        M2[AgentTeam: Leader派Worker]
        M3[高速公路: gateway不变/apps可插拔]
        M4[JWT共享密钥: Django+FastAPI]
        M5[降级策略: SSE→阻塞POST]
    end
```

### 1.5 技术栈全景

```mermaid
graph TB
    subgraph Frontend["前端技术栈 :5173"]
        Vue3["Vue 3.4<br/>Composition API"]
        Vite5["Vite 5.4<br/>构建工具"]
        EP["Element Plus 2.7<br/>UI 组件库"]
        AIV["animal-island-vue 0.2<br/>动森主题 · AI模块"]
        Pinia["Pinia 2.1<br/>状态管理"]
        Router["Vue Router 4.3<br/>SPA 路由 + 鉴权守卫"]
        Axios["Axios 1.7<br/>HTTP 客户端<br/>JWT 拦截 + 刷新"]
        Anime["animejs 4.5<br/>入场动画"]
        ECharts["ECharts 5.5<br/>数据可视化"]
        Mitt["mitt 3.0<br/>事件总线"]
        Icons["自定义 SVG 图标库<br/>24 个 · Feath icon 风格"]
    end

    subgraph Backend["Django 后端 :8765"]
        DJ["Django 4.2<br/>MTV 框架"]
        Daphne["Daphne 4.0<br/>ASGI 服务器"]
        Channels["Django Channels 4.0<br/>WebSocket"]
        Jazzmin["Jazzmin<br/>Admin 主题"]
        U2["uiautomator2 3.0<br/>Android 设备控制"]
        Pillow["Pillow 10<br/>截图处理"]
        MySQL["mysqlclient 2.2<br/>MySQL 驱动"]
        Redis["redis 5.0<br/>缓存 / Channels 后端"]
    end

    subgraph AgentScope["AgentScope AI :8000"]
        AS["AgentScope 2.0.3<br/>智能体框架"]
        FastAPI["FastAPI 0.109<br/>API 框架"]
        Uvicorn["Uvicorn 0.27<br/>ASGI 服务器"]
        APS["APScheduler 3.11<br/>Cron 调度"]
        PyJWT["pyjwt 2.8<br/>JWT 签发/验证"]
        Chroma["ChromaDB 1.5<br/>向量数据库 · RAG"]
        MCP["MCP 1.28<br/>Model Context Protocol"]
    end

    Frontend -->|"/api/* JWT"| Backend
    Frontend -->|"/agentscope/* SSE"| AgentScope
    AgentScope -->|"Tool 调用 Django ORM"| Backend
    AgentScope --> Redis
    Backend --> Redis
```

#### 前端

| 类别 | 技术 | 版本 | 用途 |
|------|------|:--:|------|
| **框架** | Vue 3 | 3.4 | Composition API + SFC |
| **构建** | Vite | 5.4 | 开发服务器 + 打包 |
| **UI 主库** | Element Plus | 2.7 | 70+ 组件：表单/表格/对话框/标签页 |
| **UI 主题** | animal-island-vue | 0.2 | AI 助手模块独立视觉：暖木色 · 大圆角 · 羊皮纸底 |
| **全局样式** | CSS 变量 + 毛玻璃 | — | `style.css` 定义 16 个设计 Token，`App.vue` 三个动态 blob |
| **状态管理** | Pinia | 2.1 | 模块级 store |
| **路由** | Vue Router | 4.3 | HTML5 History 模式 + beforeEach 鉴权守卫 |
| **HTTP** | Axios | 1.7 | 请求拦截器注入 JWT · 响应拦截器 401 刷新 |
| **动画** | animejs | 4.5 | 侧边栏入场 · 卡片 staggered · blob 视差 |
| **图表** | ECharts | 5.5 | 仪表盘数据可视化 |
| **事件总线** | mitt | 3.0 | 跨组件轻量通信 |
| **图标** | 自定义 SVG | — | 24 个 Feather 风格图标：`h()` 渲染函数，tree-shakable |
| **字体** | Inter + Quicksand | — | Google Fonts · 中英文混排栈 |

#### 后端

| 类别 | 技术 | 版本 | 用途 |
|------|------|:--:|------|
| **框架** | Django | 4.2 | MTV 架构 · ORM · Admin |
| **ASGI** | Daphne | 4.0 | HTTP + WebSocket 双协议 |
| **WebSocket** | Django Channels | 4.0 | 截图流 2fps · 测试进度推送 |
| **Admin 主题** | Jazzmin | — | Bootstrap 5 管理后台 · 自定义 CSS 字体 |
| **设备控制** | uiautomator2 | 3.0 | Python → ADB → Android 自动化 |
| **图像** | Pillow | 10.0 | 截图处理 |
| **数据库** | SQLite(dev) / MySQL(prod) | — | 环境变量 `DB_ENGINE` 切换 |
| **缓存** | Redis | 5.0 | Channels 后端 + AgentScope Storage |
| **跨域** | django-cors-headers | — | 开发环境全开放 |

#### AgentScope AI 服务

| 类别 | 技术 | 版本 | 用途 |
|------|------|:--:|------|
| **智能体框架** | AgentScope | 2.0.3 | Agent · Toolkit · Agent Team · Cron |
| **API 框架** | FastAPI | 0.109 | RESTful 端点 · 自动 OpenAPI |
| **服务器** | Uvicorn | 0.27 | ASGI 高性能 |
| **调度** | APScheduler | 3.11 | Cron 表达式 → 定时触发 Agent |
| **鉴权** | PyJWT | 2.8 | Django 共享 SECRET_KEY · dependency_overrides |
| **向量库** | ChromaDB | 1.5 | 32 篇项目文档 · RAG 检索 |
| **协议** | MCP | 1.28 | Model Context Protocol · 外部工具接入 |

#### 自定义资产

| 资产 | 数量 | 说明 |
|------|:--:|------|
| AgentScope 自定义 Tool | 14 个 | element(2) + case(3) + device(3) + runner(3) + report(2) + rag(1) |
| Agent Team 子智能体模板 | 5 个 | inspector · case-writer · device-operator · test-executor · report-writer |
| 前端业务模块 | 8 个 | 仪表盘 · 设备 · 元素定位 · 元素管理 · 用例 · 执行 · 报告 · AI助手 |
| Django App | 6 个 | 同上（无 element-manager） |
| 数据库表 | 18 张 | 6 组前缀：el_ dp_ cm_ tr_ rg_ ai_ |
| API 端点 | 55 个 | REST 55 + WebSocket 2 + AgentScope 动态 |
| JWT 端点 | 5 个 | login · register · refresh · logout · me |
| SVG 图标 | 24 个 | Dashboard · Device · Target · Brain · Play · BarChart... |

---

## 二、实现视角：当前实际架构

### 2.1 已实现 vs 裁剪清单

| 原方案（废弃） | 原因 |
|:--|:--|
| `project_hub` / `requirement_tracker` (v3) | 无实际代码，仅骨架注册 |
| `api_tester` / `data_factory` (v4) | 从未创建 |
| `shared/events/` / `shared/config/` | 空目录，无跨模块事件需求 |
| Dify API 集成 | 改为 AgentScope 直连（更灵活） |
| Celery | 改用 APScheduler（AgentScope 内置） |
| DRF Serializer/ViewSet | 保持裸 Django View（详见 2.7） |
| FastAPI `web_server.py` | 已废弃 |

| 原方案（变更） | 变更内容 |
|:--|:--|
| `ai_assistant` v4→v1 | 提前实现：6模型+18端点+AgentScope桥接 |
| JWT Auth v2→v1 | 提前实现：`shared/auth/jwt_auth.py` + 中间件 |
| `ai_sessions` → `ai_conversations` | 拆分为 6 张表 |
| `ai_dify_config` | 删除，配置合并到 AIAgent 字段 |

### 2.2 模块清单

```mermaid
graph LR
    subgraph Django["apps/ — 6 个 App"]
        EL["element_locator<br/>12端点 · 3模型"]
        DP["device_pool<br/>12端点 · 3模型"]
        CM["case_manager<br/>5端点 · 2模型"]
        TR["test_runner<br/>5端点 · 2模型"]
        RG["report_generator<br/>3端点 · 2模型"]
        AI["ai_assistant<br/>18端点 · 6模型"]
    end

    subgraph AgentScope["agentscope_service/"]
        Tool["tools/ 14个<br/>element case device<br/>runner report rag"]
        Team["teams/ 5个<br/>inspector writer operator<br/>executor writer"]
        RAG["rag/ ChromaDB<br/>32篇文档"]
    end

    subgraph Frontend["frontend/src/modules/"]
        DP2[device-pool]
        EL2[element-locator]
        EM2[element-manager]
        CM2[case-manager]
        TR2[test-runner]
        RG2[report-generator]
        AI2[ai-assistant]
    end

    Django --> Tool
    AI2 --> AgentScope
```

### 2.3 数据库表（18 张）

```mermaid
erDiagram
    el_pages ||--o{ el_elements : has
    el_pages ||--o{ el_page_flows : has
    el_elements ||--o{ el_page_flows : triggers

    dp_devices ||--o{ dp_device_locks : locked_by
    dp_devices ||--o{ dp_device_queue : queued_by

    cm_test_definitions ||--o{ cm_test_cases : cached_as

    tr_test_runs ||--o{ tr_test_results : produces

    rg_reports ||--|| rg_report_templates : uses

    ai_agents ||--o{ ai_tools : configures
    ai_agents ||--o{ ai_conversations : drives
    ai_agents ||--o{ ai_tasks : owns
    ai_agents ||--o{ ai_execution_logs : logs
    ai_conversations ||--o{ ai_messages : contains
    ai_tasks ||--o{ ai_execution_logs : traced_by
```

### 2.4 API 端点（55 个）

```mermaid
graph TB
    subgraph Auth["认证 /api/ai/auth/"]
        login["login POST"]
        register["register POST"]
        refresh["refresh POST"]
        logout["logout POST"]
        me["me GET"]
    end

    subgraph Agents["智能体 /api/ai/agents/"]
        ag_list["list GET"]
        ag_create["create POST"]
        ag_detail["detail GET"]
        ag_update["update POST"]
        ag_delete["delete POST"]
    end

    subgraph Chat["对话 /api/ai/"]
        conv_list["conversations GET"]
        conv_create["conversations/create POST"]
        msg_list["messages GET"]
        msg_send["send POST"]
        msg_save["save-message POST"]
        msg_stream["stream POST"]
        avatar["upload-avatar POST"]
    end

    subgraph Business["业务模块"]
        el["/api/elements/ 12端点"]
        dp["/api/devices/ 12端点"]
        cm["/api/cases/ 5端点"]
        tr["/api/runner/ 5端点"]
        rg["/api/reports/ 3端点"]
    end

    subgraph WS["WebSocket"]
        screenshot["/ws/screenshot 2fps"]
        test_run["/ws/test-run/{id} 进度"]
    end
```

### 2.5 数据流图

```mermaid
sequenceDiagram
    actor U as 用户
    participant Login as LoginView
    participant Django as Django:8765
    participant DB as SQLite
    participant Chat as ChatView
    participant AS as AgentScope:8000
    participant Tool as DjangoORM

    Note over U,Tool: 通道 A — 手动操作
    U->>Login: admin / admin123
    Login->>Django: POST /api/ai/auth/login
    Django->>DB: authenticate
    DB-->>Django: user=admin
    Django-->>Login: JWT token
    Login->>Chat: 跳转 AI 助手

    U->>Chat: 选择智能体
    Chat->>Django: GET /api/ai/agents
    Django-->>Chat: 智能体列表

    Note over U,Tool: 通道 B — AI 智能操作
    U->>Chat: "给登录页创建冒烟用例"
    Chat->>AS: SSE /agentscope/chat
    AS->>AS: ReAct 推理
    AS->>Tool: search_elements("登录页")
    Tool-->>AS: [Button登录, Input用户]
    AS->>Tool: save_test_case(steps=[...])
    Tool->>DB: INSERT cm_test_definitions
    DB-->>Tool: ok
    Tool-->>AS: 保存成功
    AS-->>Chat: 流式文本增量
    Chat->>Django: POST save-message 审计
```

### 2.6 AgentScope ReAct 调用链

```mermaid
sequenceDiagram
    actor U as 用户
    participant A as Agent
    participant T1 as search_elements
    participant T2 as save_test_case

    U->>A: "给登录页生成冒烟用例"
    A->>A: 思考: 需要哪些UI元素?
    A->>T1: search_elements("登录页")
    T1-->>A: Button('登录') Input('username')
    A->>A: 思考: 构造3步用例
    A->>T2: save_test_case(case_id,steps=[
    A->>T2: click(用户名输入框)
    A->>T2: click(登录按钮)
    A->>T2: wait(首页加载,timeout=10)
    A->>T2: ])
    T2-->>A: "用例已创建 id=xxx"
    A-->>U: "已创建冒烟用例: 3个步骤"
```

### 2.7 裸 Django View vs DRF（结论）

| 维度 | 裸 Django View（当前） | DRF |
|------|:--:|:--:|
| 代码量 | 少（一个函数搞定） | 多（Serializer+View+Router） |
| 请求解析 | 手动 `json.loads(request.body)` | 自动 `request.data` |
| 参数校验 | 手动 if-return-error | 声明式 Serializer |
| 序列化 | 手动 dict 拼装 | 自动 ModelSerializer |
| API 文档 | 无 | 自动 OpenAPI |
| 路由 | 手动 path() | 自动 SimpleRouter |
| 学习曲线 | 低 | 中等 |

> **结论**：保持裸 Django View。55 个端点已稳定运行，JSON 格式 `{ok, data}` 全项目统一。AgentScope Tool 直接调 Python API 不经 HTTP，DRF 无帮助。未来新建纯 RESTful App 时可局部引入。

---

## 三、部署架构

```mermaid
graph TB
    subgraph Dev["开发环境 Windows 11"]
        Redis["Redis :6379 可选"]
        Django["Django :8765 Daphne+Channels"]
        AS["AgentScope :8000 Uvicorn"]
        Vite["Vue :5173 Vite"]
    end

    Vite -->|"/api/*"| Django
    Vite -->|"/agentscope/*"| AS
    AS --> Redis
    Django --> Redis
```

```bash
# 一键启动
python run.py start      # Django + AgentScope + Vite
python run.py status     # 各服务状态
python run.py stop       # 停止全部
# 日志: logs/backend.log  logs/agentscope.log  logs/frontend.log
```
