# 前端代码质量评估 — 2026-07-16 基线

> 评估框架：10 维度 + 安全 + 组件合规 | 智能体：`frontend-evaluator`
> 代码基线：28,354 行 Vue/JS，8 模块，分支 Aaron

---

## 指标快照

| # | 指标 | 当前值 | 基准 | 趋势 |
|:--:|------|------|:--:|:--:|
| 1 | 超 500 行 .vue 文件 | **18 个** | ≤5 | 🔴 |
| 2 | 最高单文件行数 | **1098** (test-runner/index.vue) | 500 | 🔴 |
| 3 | 静默吞错 `catch(_)` | **~55 处** | 0 | 🔴 |
| 4 | 裸 client 调用（组件内） | **~20 处** | 0 | 🔴 |
| 5 | CSS 变量使用率 | **26%** (388/1484) | ≥60% | 🔴 |
| 6 | 组件 API 违规 | **0 处** | 0 | ✅ |
| 7 | 硬编码凭据 | **已修复** (LoginView.vue) | 0 | ✅ |
| 8 | 硬编码假数据 `ref([{` | **0 处** | 0 | ✅ |
| 9 | 路由懒加载覆盖率 | **100%** (17/17) | 100% | ✅ |
| 10 | 模块常量外提率 | **4/8** | 8/8 | 🟡 |
| 11 | 测试文件数 | **0** | ≥5 | 🔴 |

---

## 维度评分

| 维度 | 评分 | 核心指标 |
|------|:--:|------|
| 路由 | ✅ | 懒加载 100%、404 页面✅、JWT 守卫✅、meta.title✅ |
| 状态管理 | 🟡 | Pinia/composable 边界清晰，16 处裸 localStorage，useStorage 仅 2 处 |
| API 层 | 🟡 | shared/endpoints.js 已建，4 种命名风格并存，useApi 0 引用 |
| 组件 | 🟡 | 归属合理，18 个超 500 行文件是主要问题 |
| 样式 | 🟡 | CSS 变量 26%，16 个文件 scoped >200 行未外置 |
| 常量 | 🟡 | 4/8 模块已有 constants.js |
| 错误处理 | 🔴 | 三层体系已建但 useApi 0% 使用率，~55 处静默吞错 |
| 构建 | ✅ | 5 组 manualChunks、按需引入✅，缺打包分析 |
| 测试 | 🔴 | 0 测试文件、0 测试依赖 |
| 类型 | 🟢 | 4 composable JSDoc✅，workflow 使用 TS |

---

## 问题清单

### P1 — 高优先级（7 项）

| # | 文件 | 问题 | 违反规则 |
|---|------|------|---------|
| P1-1 | `test-runner/index.vue` 等 18 个 | 超 500 行上限 | conventions.md |
| P1-2 | `device-pool/store.js` (6处) | 写操作静默吞错 | frontend.md |
| P1-3 | `case-manager/CaseEditor.vue` (4处) | 含设备连接、用例保存静默吞错 | frontend.md |
| P1-4 | `test-runner/index.vue` (6处) | 裸调 client 未走 api.js | api-conventions.md |
| P1-5 | `case-manager/StepEditor.vue` (4处) | 裸调 client 未走 api.js | api-conventions.md |
| P1-6 | `case-manager/index.vue` (5处) | 直接 localStorage 而非 useStorage | frontend.md |
| P1-7 | `shared/endpoints.js` | 已创建但零引用，4 个模块仍各自定义 `/devices` | api-conventions.md |

### P2 — 中优先级（5 项）

| # | 问题 | 影响面 |
|---|------|------|
| P2-1 | CSS 变量使用率仅 26% | 全局 |
| P2-2 | 4 模块缺 constants.js (dashboard/element-locator/case-manager/ai-assistant) | 4 模块 |
| P2-3 | localStorage key 格式不统一（`dp_user_id` vs `case-manager-view-mode`） | 散落 |
| P2-4 | `router.js` 直接 `localStorage.getItem` 而非 import `getToken()` | 1 处 |
| P2-5 | report-generator 子页面 (ReportDetail/CaseBreakdown/TaskReport) 应移入 components/ | 3 个文件 |

---

## 改进路线图

```
本周  ▸ P1-2~3 写操作静默吞错修复 (~20 处)         2h
      ▸ P1-6 case-manager localStorage → useStorage   30min

本月  ▸ P1-1 拆分 TOP 5 超限文件                     8h
      ▸ P1-4~5 裸 client → shared/endpoints 迁移     3h
      ▸ P1-7 各模块 api.js 引用 shared/endpoints      1h
      ▸ P2-2 余下 4 模块建 constants.js               1h
      ▸ 打包分析 rollup-plugin-visualizer             15min

季度  ▸ Vitest + 5 个 composable 测试                4h
      ▸ CSS 变量化 (目标 60%+)                        6h
      ▸ device-pool style 外置                         30min
```

---

## 评估元数据

- 评估智能体：`.claude/agents/frontend-evaluator.md`
- 评估方案：`.claude/plans/precious-strolling-shell.md`（10 要素）
- 规则文件：`.claude/rules/frontend.md` `security.md` `animal-island-ui.md` `api-conventions.md` `conventions.md`
- 下次评估：代码修改后运行 `frontend-evaluator` agent，与此基线对比
