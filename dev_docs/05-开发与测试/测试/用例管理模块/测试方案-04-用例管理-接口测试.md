# 用例管理模块 — 接口测试用例

> 关联：[API-03-用例管理](../../02-PRD需求/API-03-用例管理.md) · [PRD-03](../../02-PRD需求/PRD-03-用例管理.md)
> 自动化：`tests/test_case_manager_api.py` · 版本：v1.1 · 日期：2026-07-17

---

## 测试环境

| 项目 | 值 |
|------|------|
| 基础 URL | `http://localhost:8765/api/cases` |
| 鉴权方式 | JWT Bearer Token |
| 测试账号 | admin / admin123 |
| 辅助账号 | tester / tester123（部分用例需要） |

---

## 1. 目录管理 — 9 条

### CM-DIR-01：列出目录树

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/directories` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | 已有至少 1 个目录 |
| **期望状态码** | `200` |
| **期望响应** | `tree` 为非空数组，节点含 `id/name/created_by/allow_create/allow_delete` |

---

### CM-DIR-02：创建根级目录

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/create` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"name":"接口测试目录","parent_id":null,"sort_order":0}` |
| **前置** | 无同名目录 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"directory":{"id":<int>,"name":"接口测试目录"}}` |

---

### CM-DIR-03：同名目录拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/create` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"name":"接口测试目录","parent_id":null}` |
| **前置** | CM-DIR-02 已创建同名目录 |
| **期望状态码** | `400` |
| **期望响应** | `{"status":false,"message":"该层级下已存在同名目录: 接口测试目录"}` |

---

### CM-DIR-04：重命名目录

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"action":"update","name":"接口测试目录(已重命名)"}` |
| **前置** | CM-DIR-02 产出 `dir_id` |
| **期望状态码** | `200` |

---

### CM-DIR-05：删除空目录

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"action":"delete"}` |
| **前置** | 目录下无用例和子目录 |
| **期望状态码** | `200` |

---

### CM-DIR-06：删除非空目录拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"action":"delete"}` |
| **前置** | 目录下有至少 1 条用例或子目录 |
| **期望状态码** | `400` |
| **期望响应** | `error` 含 "请先清空" |

---

### CM-DIR-07：他人删除目录拒绝（权限检查）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}` |
| **请求头** | `Authorization: Bearer <tester_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"action":"delete"}` |
| **前置** | `dir_id` 为 admin 创建的目录 |
| **期望状态码** | `403` |
| **期望响应** | `error` 含 "只有目录创建者" |

---

### CM-DIR-08：设置目录权限（允许创建=否）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}/permission` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"allow_create":false,"allow_delete":true}` |
| **前置** | 当前用户为目录创建者 |
| **期望状态码** | `200` |

---

### CM-DIR-09：非创建者改目录权限拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/directories/{dir_id}/permission` |
| **请求头** | `Authorization: Bearer <tester_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"allow_create":true,"allow_delete":true}` |
| **前置** | `dir_id` 为 admin 创建的目录 |
| **期望状态码** | `403` |
| **期望响应** | `error` 含 "只有目录创建者" |

---

## 2. 协作锁 — 7 条

### CM-LOCK-01：获取编辑锁

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/lock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | 用例存在，未被编辑，非只读 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"editing_by":"admin","editing_since":"<iso>"}` |

---

### CM-LOCK-02：重复获取锁（幂等）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/lock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | CM-LOCK-01 已持有锁 |
| **期望状态码** | `200` |
| **期望响应** | `ok=true`（刷新 `editing_since` 时间） |

---

### CM-LOCK-03：编辑锁冲突（他人持有）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/lock` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | admin 持有编辑锁（CM-LOCK-01） |
| **期望状态码** | `423` |
| **期望响应** | `{"status":false,"message":"用例正被 admin 编辑中","editing_by":"admin"}` |

---

### CM-LOCK-04：释放编辑锁

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/unlock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | `{}` |
| **前置** | admin 持有编辑锁 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"released":true}` |

---

### CM-LOCK-05：强制踢出（创建者）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/unlock` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"force":true}` |
| **前置** | admin 是创建者，tester 持有锁 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"force_unlocked":true}` |

---

### CM-LOCK-06：强制踢出拒绝（非创建者）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/unlock` |
| **请求头** | `Authorization: Bearer <tester_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"force":true}` |
| **前置** | admin 是创建者，admin 持有锁 |
| **期望状态码** | `403` |
| **期望响应** | `error` 含 "只有用例创建者可以强制解除编辑锁" |

---

### CM-LOCK-07：释放未锁定的锁（幂等）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/unlock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | `{}` |
| **前置** | 用例未被任何人锁定 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"already_unlocked":true}` |

---

## 3. 持久锁 — 4 条

### CM-PLOCK-01：创建者锁定用例

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/case-lock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | admin 是创建者 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"locked":true}` |

---

### CM-PLOCK-02：非创建者锁定拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/case-lock` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | admin 是创建者 |
| **期望状态码** | `403` |
| **期望响应** | `error` 含 "只有创建者可以锁定" |

---

### CM-PLOCK-03：创建者解除锁定

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/case-unlock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | 用例已被 admin 锁定 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"unlocked":true}` |

---

### CM-PLOCK-04：未锁定时解除（幂等）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/case-unlock` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | 用例未被锁定 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"already_unlocked":true}` |

---

## 4. 可见性 — 5 条

### CM-VIS-01：设为仅创建者可见

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/visibility` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"visibility":"hidden"}` |
| **前置** | admin 是创建者 |
| **期望状态码** | `200` |

---

### CM-VIS-02：hidden 用例对他人列表不可见

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | admin 已将用例设为 `visibility=hidden` |
| **期望状态码** | `200` |
| **期望响应** | `definitions` 数组中不包含该用例 ID |

---

### CM-VIS-03：hidden 用例直接 URL 访问拒绝

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions/{case_id}` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | 用例 `visibility=hidden`，`created_by=admin` |
| **期望状态码** | `404` |
| **期望响应** | `{"status":false,"message":"not found"}` |

---

### CM-VIS-04：设为指定用户可见

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/visibility` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"visibility":"restricted","permitted_users":["tester"]}` |
| **前置** | admin 是创建者 |
| **期望状态码** | `200` |

---

### CM-VIS-05：restricted 对指定用户列表可见

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | CM-VIS-04 已设为 restricted + tester |
| **期望状态码** | `200` |
| **期望响应** | `definitions` 数组中包含该用例 |

---

## 5. 编辑权限 — 4 条

### CM-PERM-01：创建只读用例

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"只读测试","package_name":"com.test","permission":"readonly","steps_data":[]}` |
| **期望状态码** | `200` |

---

### CM-PERM-02：只读用例拒绝非创建者 lock

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/lock` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | 用例 `permission=readonly`，`created_by=admin` |
| **期望状态码** | `423` |
| **期望响应** | `{"status":false,"message":"此用例为只读模式，仅创建者可编辑"}` |

---

### CM-PERM-03：创建指定用户可编辑用例

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"指定编辑测试","package_name":"com.test","permission":"restricted","permitted_editors":["tester"],"steps_data":[]}` |
| **期望状态码** | `200` |

---

### CM-PERM-04：未授权用户 lock 被拒

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/lock` |
| **请求头** | `Authorization: Bearer <user3_token>` |
| **请求体** | 无 |
| **前置** | 用例 `permission=restricted`，`permitted_editors=["tester"]`，user3 不在列表中 |
| **期望状态码** | `423` |
| **期望响应** | `{"status":false,"message":"此用例仅限指定用户编辑"}` |

---

## 6. 协作追踪 — 3 条

### CM-TRACK-01：创建人自动记录

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions/{case_id}` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | admin 创建了该用例 |
| **期望状态码** | `200` |
| **期望响应** | `definition.created_by = "admin"`，`definition.created_at` 非空 |

---

### CM-TRACK-02：修改人自动更新

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions/{case_id}` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | tester 刚编辑保存了 admin 创建的该用例 |
| **期望状态码** | `200` |
| **期望响应** | `definition.updated_by = "tester"` |

---

### CM-TRACK-03：编辑状态同步

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | admin 对某用例执行了 lock |
| **期望状态码** | `200` |
| **期望响应** | 该用例的 `editing_by = "admin"` |

---

## 7. 数据同步 — 3 条

### CM-SYNC-01：保存后列表更新

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | 刚通过 POST 修改了某用例的标题 |
| **期望状态码** | `200` |
| **期望响应** | 列表中该用例的 `title` 已更新为新值 |

---

### CM-SYNC-02：锁定后列表同步

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | admin 刚对某用例执行了 case-lock |
| **期望状态码** | `200` |
| **期望响应** | 该用例的 `locked = true` |

---

### CM-SYNC-03：乐观锁冲突

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <tester_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"id":"<case_id>","title":"冲突测试-B","package_name":"com.test","steps_data":[],"updated_at":"<旧的 updated_at>"}` |
| **前置** | admin 已先保存该用例（updated_at 已更新） |
| **期望状态码** | `409` |
| **期望响应** | `error` 含 "已被他人修改" |

---

## 8. 用例 CRUD — 5 条

### CM-API-01：创建用例（自动生成 ID）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"自动ID测试","package_name":"com.test","steps_data":[{"type":"click","xpath":"//Button"}],"directory_id":null}` |
| **前置** | 不传 `id` 字段 |
| **期望状态码** | `200` |
| **期望响应** | `id` 匹配格式 `TC-YYYYMMDD-HHMMSS-NNNN` |

---

### CM-API-02：创建用例（指定 ID）

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"id":"TC-custom-001","title":"指定ID测试","package_name":"com.test","steps_data":[]}` |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"id":"TC-custom-001"}` |

---

### CM-API-03：同目录标题重复

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"指定ID测试","package_name":"com.test","steps_data":[],"directory_id":<CM-API-02的dir_id>}` |
| **前置** | CM-API-02 已创建同目录同标题用例 |
| **期望状态码** | `409` |
| **期望响应** | `error` 含 "已存在同名用例" |

---

### CM-API-04：更新用例

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"id":"<CM-API-02的id>","title":"指定ID测试(改)","package_name":"com.test","steps_data":[],"updated_at":"<CM-API-02响应中的updated_at>"}` |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true,"id":"TC-custom-001"}` |

---

### CM-API-05：删除用例

| 项 | 值 |
|------|------|
| **方法** | `DELETE` |
| **路径** | `/api/cases/definitions/{case_id}` |
| **请求头** | `Authorization: Bearer <admin_token>` |
| **请求体** | 无 |
| **前置** | `case_id` 存在 |
| **期望状态码** | `200` |
| **期望响应** | `{"status":true}` |
| **后置验证** | 再次 `GET /definitions/{case_id}` → `404` |

---

## 9. 安全 — 3 条

### CM-SEC-01：无鉴权请求拒绝

| 项 | 值 |
|------|------|
| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | 无 `Authorization` |
| **请求体** | 无 |
| **期望状态码** | `401` |

---

### CM-SEC-02：非创建者改可见性拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/visibility` |
| **请求头** | `Authorization: Bearer <tester_token>`<br>`Content-Type: application/json` |
| **请求体** | `{"visibility":"hidden"}` |
| **前置** | `case_id` 的创建者为 admin |
| **期望状态码** | `403` |
| **期望响应** | `error` 含 "只有创建者" |

---

### CM-SEC-03：非创建者改权限拒绝

| 项 | 值 |
|------|------|
| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{case_id}/case-lock` |
| **请求头** | `Authorization: Bearer <tester_token>` |
| **请求体** | 无 |
| **前置** | `case_id` 的创建者为 admin |
| **期望状态码** | `403` |

---

## 10. 业务交互流 — 7 条

> 多接口串联，验证参数在接口间正确传递，覆盖完整用户场景。

---

### CM-FLOW-01：创建→编辑→锁定→删除（全生命周期）

| 步骤 | 方法 | 路径 | 请求头 | 请求体 | 期望 | 产出参数 |
|:--:|------|------|------|------|:--:|------|
| 1 | `GET` | `/directories` | `Authorization: Bearer <admin>` | 无 | `200`，取 `tree[0].id` | `dir_id` |
| 2 | `POST` | `/definitions` | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` | `{"title":"流程测试","package_name":"com.test","steps_data":[...],"directory_id":<dir_id>}` | `200`，取 `id` | `case_id` |
| 3 | `GET` | `/definitions/{case_id}` | `Authorization: Bearer <admin>` | 无 | `200`，取 `updated_at` | `ts1` |
| 4 | `POST` | `/definitions/{case_id}/lock` | `Authorization: Bearer <admin>` | 无 | `200`，`editing_by="admin"` | — |
| 5 | `POST` | `/definitions` | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` | `{"id":<case_id>,"title":"流程测试(改)","package_name":"com.test","steps_data":[],"updated_at":<ts1>}` | `200` | — |
| 6 | `POST` | `/definitions/{case_id}/unlock` | `Authorization: Bearer <admin>` | `{}` | `200` | — |
| 7 | `DELETE` | `/definitions/{case_id}` | `Authorization: Bearer <admin>` | 无 | `200` | — |
| 8 | `GET` | `/definitions/{case_id}` | `Authorization: Bearer <admin>` | 无 | `404` | — |

---

### CM-FLOW-02：两人协作完整交互

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 2 | tester | `GET` | `/definitions` | 无 | `editing_by="admin"` |
| 3 | tester | `POST` | `/definitions/{id}/lock` | 无 | `423` "正被 admin 编辑中" |
| 4 | admin | `POST` | `/definitions/{id}/unlock` | `{}` | `200` |
| 5 | tester | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 6 | tester | `POST` | `/definitions/{id}/unlock` | `{}` | `200` |

---

### CM-FLOW-03：创建者强制踢出

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | tester | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 2 | admin | `GET` | `/definitions/{id}` | 无 | `created_by="admin"`, `editing_by="tester"` |
| 3 | admin | `POST` | `/definitions/{id}/unlock` | `{"force":true}` | `200`, `force_unlocked=true` |
| 4 | admin | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 5 | tester | `GET` | `/definitions` | 无 | `editing_by="admin"` |

---

### CM-FLOW-04：权限控制流（只读→指定→恢复）

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions` | `{"title":"权限流","package_name":"com.test","permission":"readonly","steps_data":[]}` | `200` |
| 2 | tester | `POST` | `/definitions/{id}/lock` | 无 | `423` "只读模式" |
| 3 | admin | `POST` | `/definitions` | `{"id":<id>,"permission":"restricted","permitted_editors":["tester"],...}` | `200` |
| 4 | tester | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 5 | tester | `POST` | `/definitions/{id}/unlock` | `{}` | `200` |
| 6 | admin | `POST` | `/definitions` | `{"id":<id>,"permission":"edit",...}` | `200` |

---

### CM-FLOW-05：可见性控制流

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions` | `{"title":"可见性流","package_name":"com.test","visibility":"public","steps_data":[]}` | `200` |
| 2 | tester | `GET` | `/definitions` | 无 | 列表中包含该用例 |
| 3 | admin | `POST` | `/definitions/{id}/visibility` | `{"visibility":"hidden"}` | `200` |
| 4 | tester | `GET` | `/definitions` | 无 | 列表中**不**包含该用例 |
| 5 | tester | `GET` | `/definitions/{id}` | 无 | `404` |
| 6 | admin | `POST` | `/definitions/{id}/visibility` | `{"visibility":"restricted","permitted_users":["tester"]}` | `200` |
| 7 | tester | `GET` | `/definitions` | 无 | 列表中包含该用例 |

---

### CM-FLOW-06：目录权限完整流

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/directories/create` | `{"name":"权限目录"}` | `200`，`created_by="admin"` |
| 2 | admin | `POST` | `/directories/{id}/permission` | `{"allow_create":false,"allow_delete":false}` | `200` |
| 3 | tester | `GET` | `/directories` | 无 | `allow_create=false` |
| 4 | tester | `POST` | `/directories/{id}` | `{"action":"delete"}` | `403` |
| 5 | admin | `POST` | `/directories/{id}/permission` | `{"allow_delete":true}` | `200` |
| 6 | tester | `POST` | `/directories/{id}` | `{"action":"delete"}` | `200` |

---

### CM-FLOW-07：批量导入导出流

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/export/yaml` | `{"test_case_name":"导出测试"}` | `200`，取 `filename` |
| 2 | admin | `GET` | `/exports/{filename}` | 无 | YAML 文件流 |
| 3 | admin | `GET` | `/exports` | 无 | 文件列表含该文件 |
| 4 | admin | `POST` | `/definitions/batch` | `{"cases":[{...}],"overwrite":false}` | `200` |

---

## 汇总

| 分组 | 编号 | 条数 | 覆盖新增端点 |
|------|------|:--:|------|
| 目录管理 | CM-DIR-01 ~ 09 | 9 | directories, create, update/delete, permission |
| 协作锁 | CM-LOCK-01 ~ 07 | 7 | lock, unlock |
| 持久锁 | CM-PLOCK-01 ~ 04 | 4 | case-lock, case-unlock |
| 可见性 | CM-VIS-01 ~ 05 | 5 | visibility |
| 编辑权限 | CM-PERM-01 ~ 04 | 4 | lock（权限检查） |
| 协作追踪 | CM-TRACK-01 ~ 03 | 3 | definitions（响应字段验证） |
| 数据同步 | CM-SYNC-01 ~ 03 | 3 | definitions（乐观锁、同步） |
| 用例 CRUD | CM-API-01 ~ 05 | 5 | definitions, detail, delete |
| 安全 | CM-SEC-01 ~ 03 | 3 | 401/403 验证 |
| **业务交互流** | CM-FLOW-01 ~ 07 | **7 条流程 (38 步骤)** | 全端点串联 |
| 参数校验 | CM-ARG-01 ~ 08 | 8 | 边界值/特殊字符/非法枚举 |
| 端点补全 | CM-END-01 ~ 06 | 6 | batch-move/batch/export/exports/delete |
| 幂等性 | CM-IDEM-01 ~ 03 | 3 | 重复锁定/重复删除/重复设权限 |
| 业务中断流 | CM-FLOW-08 ~ 10 | 3 | 编辑冲突/权限变更中断/可见性中途变更 |
| 并发冲突 | CM-CONC-01 ~ 02 | 2 | 同时创建同名/同时 lock |
| 非功能 | CM-NF-01 ~ 03 | 3 | Content-Type/空body/响应头 |
| **合计** | | **43 + 7 流程 + 25 补充 = 75** | |

---

## 运行方式

```bash
# 1. 获取 admin token
ADMIN=$(curl -s -X POST http://localhost:8765/api/ai/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

# 2. 单条示例：CM-DIR-01 列出目录树
curl -s http://localhost:8765/api/cases/directories \
  -H "Authorization: Bearer $ADMIN" | python -m json.tool

# 3. 运行全部自动化测试
python tests/test_case_manager_api.py
```

---

## 附录A：补充用例（审计后新增 25 条）

> 来源：[审计报告](./测试方案-04-用例管理-接口测试-审计报告.md) · 六维模型覆盖率 68% → 95%+

### A1. 参数校验 — 8 条

#### CM-ARG-01：目录名超长

| **方法** | `POST` |
| **路径** | `/api/cases/directories/create` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"name":"<201个字符>"}` |
| **期望状态码** | `500`（数据库字段溢出） |

#### CM-ARG-02：目录名为空

| **方法** | `POST` |
| **路径** | `/api/cases/directories/create` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"name":"","parent_id":null}` |
| **期望状态码** | `400` 或 `200`（取决于是否校验空字符串） |

#### CM-ARG-03：目录名含特殊字符

| **方法** | `POST` |
| **路径** | `/api/cases/directories/create` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"name":"<script>alert(1)</script>","parent_id":null}` |
| **期望状态码** | `200`（XSS 由前端防护，后端原样存储） |

#### CM-ARG-04：用例 title 超长

| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"<501个字符>","package_name":"com.test","steps_data":[]}` |
| **期望状态码** | `500` |

#### CM-ARG-05：非法 priority 值

| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"非法优先级","package_name":"com.test","priority":"P99","steps_data":[]}` |
| **期望状态码** | `200`（Django choices 静默降级为 P1） |

#### CM-ARG-06：非法 visibility 值

| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{id}/visibility` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"visibility":"deleted"}` |
| **期望状态码** | `200`（静默降级） |

#### CM-ARG-07：GET 不存在用例 404

| **方法** | `GET` |
| **路径** | `/api/cases/definitions/TC-nonexistent` |
| **请求头** | `Authorization: Bearer <admin>` |
| **请求体** | 无 |
| **期望状态码** | `404` |

#### CM-ARG-08：缺 package_name

| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"title":"缺包名","steps_data":[]}` |
| **期望状态码** | `200`（package_name 非必填，default=""） |

---

### A2. 端点补全 — 6 条

#### CM-END-01：批量移动 — 正常 + 异常

| 步骤 | **方法** | **路径** | **请求体** | **期望** |
|:--:|------|------|------|:--:|
| 1 | `POST` | `/directories/batch-move` | `{"items":[{"type":"case","id":"<case_id>"}],"target_directory_id":<dir_id>}` | `200` |
| 2 | `POST` | `/directories/batch-move` | `{"items":[],"target_directory_id":1}` | `400` "items 必须是非空数组" |
| 3 | `POST` | `/directories/batch-move` | `{"items":[{"type":"case","id":"x"}],"target_directory_id":99999}` | `500` |

#### CM-END-02：批量导入

| 步骤 | **方法** | **路径** | **请求体** | **期望** |
|:--:|------|------|------|:--:|
| 1 | `POST` | `/definitions/batch` | `{"cases":[{...3条合法用例...}],"overwrite":false}` | `200`，`imported` 含 3 个 ID |
| 2 | `POST` | `/definitions/batch` | `{"cases":[{...}],"overwrite":false}` (重复 ID) | `200`，计入 `skipped` |
| 3 | `POST` | `/definitions/batch` | `{"cases":[{...}],"overwrite":true}` (重复 ID) | `200`，计入 `imported` |
| 4 | `POST` | `/definitions/batch` | `{"cases":[{...501条...}]}` | `400` |

#### CM-END-03：导出 YAML

| **方法** | `POST` |
| **路径** | `/api/cases/export/yaml` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"test_case_name":"","page_ids":[]}` |
| **期望状态码** | `200`，`filename` 以 `auto_test_` 或 `` 开头 |

#### CM-END-04：导出文件列表（空 + 有文件）

| 步骤 | **方法** | **路径** | **期望** |
|:--:|------|------|------|
| 1 | `GET` | `/exports` | `200`，`files` 为数组（可为空） |
| 2 | 先执行导出 | 再 `GET /exports` | `files` 包含刚导出的文件 |

#### CM-END-05：下载不存在的导出文件

| **方法** | `GET` |
| **路径** | `/api/cases/exports/nonexistent.yaml` |
| **请求头** | 无 |
| **请求体** | 无 |
| **期望状态码** | `404` |

#### CM-END-06：删除不存在用例（幂等）

| **方法** | `DELETE` |
| **路径** | `/api/cases/definitions/TC-nonexistent` |
| **请求头** | `Authorization: Bearer <admin>` |
| **请求体** | 无 |
| **期望状态码** | `200`（幂等，删除不存在的资源不报错） |

---

### A3. 幂等性 — 3 条

#### CM-IDEM-01：重复 case-lock（幂等）

| **方法** | `POST` |
| **路径** | `/api/cases/definitions/{id}/case-lock` |
| **请求头** | `Authorization: Bearer <admin>` |
| **请求体** | 无 |
| **前置** | 该用例已被 admin case-lock |
| **期望状态码** | `200` |

#### CM-IDEM-02：重复删除已删除用例

同 CM-END-06（已覆盖）

#### CM-IDEM-03：重复设相同目录权限

| **方法** | `POST` |
| **路径** | `/api/cases/directories/{id}/permission` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{"allow_create":false,"allow_delete":true}` |
| **前置** | 已设过相同值 |
| **期望状态码** | `200` |

---

### A4. 业务中断流 — 3 条

#### CM-FLOW-08：编辑冲突完整交互

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions/{id}/lock` | 无 | `200` |
| 2 | tester | `POST` | `/definitions/{id}/lock` | 无 | `423` |
| 3 | admin | `POST` | `/definitions` (保存) | `{...}` | `200` |
| 4 | admin | `POST` | `/definitions/{id}/unlock` | `{}` | `200` |
| 5 | tester | `POST` | `/definitions/{id}/lock` | 无 | `200` |

#### CM-FLOW-09：权限变更中断恢复

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions` | `{"permission":"readonly",...}` | `200` |
| 2 | tester | `POST` | `/definitions/{id}/lock` | 无 | `423` |
| 3 | admin | `POST` | `/definitions` (更新) | `{"id":<id>,"permission":"edit",...}` | `200` |
| 4 | tester | `POST` | `/definitions/{id}/lock` | 无 | `200` |

#### CM-FLOW-10：可见性中途变更

| 步骤 | 角色 | 方法 | 路径 | 请求体 | 期望 |
|:--:|:--:|------|------|------|:--:|
| 1 | admin | `POST` | `/definitions` | `{"visibility":"public",...}` | `200` |
| 2 | tester | `GET` | `/definitions` | 无 | 包含该用例 |
| 3 | admin | `POST` | `/definitions/{id}/visibility` | `{"visibility":"hidden"}` | `200` |
| 4 | tester | `GET` | `/definitions` | 无 | **不**包含该用例 |

---

### A5. 并发冲突 — 2 条

#### CM-CONC-01：同时创建同目录同名（需要自动化脚本并发）

| **场景** | 两个请求同时 `POST /definitions`，相同 `directory_id` + `title` |
| **期望** | 一个 `200` + 一个 `409`（或两个中一个因 DB unique constraint 失败） |

#### CM-CONC-02：同时 lock 同一用例

| **场景** | admin 和 tester 同时 `POST /definitions/{id}/lock` |
| **期望** | 一个 `200` + 一个 `423` |

---

### A6. 非功能 — 3 条

#### CM-NF-01：POST 缺 Content-Type

| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>`（**无** Content-Type） |
| **请求体** | 原始 JSON 字符串 |
| **期望状态码** | `400` 或 `200`（取决于 Django 解析行为） |

#### CM-NF-02：POST 空 body

| **方法** | `POST` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>`<br>`Content-Type: application/json` |
| **请求体** | `{}` |
| **期望状态码** | `200`（生成 ID，title="" 但不校验）或 `400` |

#### CM-NF-03：响应 Content-Type 检查

| **方法** | `GET` |
| **路径** | `/api/cases/definitions` |
| **请求头** | `Authorization: Bearer <admin>` |
| **请求体** | 无 |
| **期望** | 响应头 `Content-Type: application/json` |
