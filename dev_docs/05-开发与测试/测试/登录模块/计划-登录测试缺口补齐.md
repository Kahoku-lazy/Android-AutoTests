# 登录测试缺口补齐（集成层）Implementation Plan

**Goal:** 按建议补齐拦截器集成测、切账号 /me、跨 Tab、黑名单债登记、CI Vitest。

## Tasks

- [x] T1 抽出 `api-auth-interceptors` + Vitest P0
- [x] T2 E2E 切账号后 `/me` 身份
- [x] T3 E2E 跨 Tab 登出/加号
- [x] T4 README 登记 logout/refresh 黑名单债（保持 xfail）
- [x] T5 CI 增加 `npm test`；文档说明 API/E2E 本地跑
