## 测试用例添加弹窗检查机制
  1. 一种是全局弹窗，这个为前置条件，在步骤编排上设置这个全局功能。新建组件“前置条件”功能栏，来放这些步骤。只有全局机制的步骤才允许选择。先不写功能只留出
  位置。
  2. 一种是弹窗文本检查，这个可以在步骤编排中设置步骤，当前添加一个toasl弹窗文本检测功能。
  ## 测试用例添加判断分支语法，步骤编排语法采用缩进，就可以编程一样，只执行这个缩进内的步骤
  1. 语法格式是如果某元素出现，则点击此元素
  2. 如果某元素消失，则点击这个元素
  3. 如果某元素出现，执行如下步骤，步骤编排语法采用缩进，就可以编程一样，只执行这个缩进内的步骤
  4. 如果某元素消失，执行如下步骤，步骤编排语法采用缩进，就可以编程一样，只执行这个缩进内的步骤
  ## 循环功能
  1. 以下步骤，循环执行N次，步骤编排语法采用缩进，就可以编程一样，只执行这个缩进内的步骤
  2. 循环点击元素列表中元素，直到全部点击完成。步骤编排语法采用缩进，就可以编程一样，只执行这个缩进内的步骤