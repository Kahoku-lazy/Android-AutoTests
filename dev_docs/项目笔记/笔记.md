## Vue

### 代码验证
1. npx vue-tsc 验证类型改动,
2. npx vue-tsc --noEmit
    1. 属性/字段不存在（TS2339）
    2. 2. 类型不匹配赋值（TS2322）
    3. 参数个数/签名不符（TS2554）
    4. 模板表达式的类型上下文
    5. 间接暴露的运行时 bug（它只报类型错，但根因是逻辑错）
3. 


## Django 类型检查问题
1. 
    pip install mypy django-stubs
    # mypy.ini 配 django_settings_module = config.settings
    mypy apps/
    python -m mypy apps/

2. 用 basedpyright（和 Cursor 里报错同源，推荐）
    pip install basedpyright
    basedpyright apps/accounts apps/ shared/
    # 或全量
    basedpyright apps/
---------------------------------------------------------------------------------------------

执行引擎问题：调度器

ARCH-00-平台总体架构.md

1. gen_arch_stats.py 少算 3 张表：它只扫 models.py 的 db_table，漏掉 case_manager 的 models_api/web/storage.py（cm_api_testcases 等 3 张），所以它报「32 表」是错的，实际 35 表。我在文档里用了准确的 35，没沿用工具的错误数字。
2. dev_docs/项目笔记/笔记.md 里有一行「执行引擎问题：调度器 → ARCH-00-平台总体架构.md」（这是会话开始前就存在的未提交改动，不是我改的）。它正好印证了两点：你之前就规划过 ARCH-00-平台总体架构.md 这个文件名（命名一致），且「执行引擎缺 scheduler 模块」是已知待办。现在该引用指向的文件已存在，链接是通的。

## 附录A：测试优先级

| 优先级 | 覆盖范围 | 验收时机 |
|:--:|------|------|
| P0 | F-01-01~F-01-02（统计+趋势） | 每次 MR 前 |
| P1 | F-02-02（最近动态） | 发版前 |

## 附录B：实施状态

核心功能已实现；存在附录C 所列已知问题与前后端契约 gap。已移除「模块入口」（ModuleNavigator）区块（2026-08-13）。

## 附录C：已知问题与改进项

| 编号 | 问题 | 严重度 | 记录日期 |
|:--:|------|:--:|:--:|
| IMP-01 | 仪表盘缺少自动刷新。页面仅在 `onMounted` 和手动点击"刷新"按钮时更新数据，无 `setInterval` 轮询/WebSocket/`onActivated` | 🟡 | 2026-07-25 |
| IMP-02 | 任务结果卡片跳转：已完成任务使用 `case_id` 而非 `client_task_id`，临时降级为跳转 `/runner` | 🟠 | 2026-07-25 |
| IMP-03 | 大屏（>1600px）留白过大，已通过 `max-width: none` 修复 | 🟢 | 2026-07-25 |
| IMP-04 | `ActivityTimeline` 事件 type：后端返回 `run`/`agent`，前端类型定义为 `success`/`warning`/`error`/`info`，时间线着色失效 | 🟠 | 2026-08-13 |
| IMP-05 | `dashboard_stats` 返回 `pass_rate`、`charts.devices/cases/pass_rate`、`elements.breakdown` 前端未消费（冗余返回） | 🟡 | 2026-08-13 |


--------------

# DRF 迁移方案 — evaluator 试点

## 目标

将项目从纯 Django 函数视图向 Django REST Framework 迁移，试点选 **evaluator** App。

目标：**规范化架构 + API 自动文档 + 减少重复代码**。


## Before → After

### 1. 序列化：手写校验 → Serializer 声明式

**Before** — 每个 View 手动 `json.loads` + 逐个字段校验 + 手拼 dict 响应：


### 2. 权限：命令式 if → 声明式配置

**Before** — 每个 View 手动重复鉴权检查：

**After** — 全局声明，所有 ViewSet 自动鉴权：

### 3. 路由：14 条 path() → 2 行 register()

**Before** — 44 行手动路由注册：

**After** — 12 行。Router 自动生成标准 REST 路由，`@action` 处理非标准操作，KB 端点保留原样：

---

## 实施步骤

### Step 1: 安装依赖


### Step 2: 全局配置

### Step 3: JWT → DRF 认证桥接

### Step 4: 新建 Serializers

### Step 5: 新建 ViewSets

### Step 6: 更新路由

### Step 7: 验证

---

## 行数对比

| 文件 | Before | After | 变化 |
|------|:--:|:--:|:--:|
| `views.py` | 545 行 | ~100 行（仅保留 kb_search/kb_self_test/frameworks） |
| `urls.py` | 44 行 | ~14 行 |
| `serializers.py` | 不存在 | ~60 行（新增） |
| `views_api.py` | 不存在 | ~140 行（新增） |
| **合计** | **589 行** | **~314 行（-47%）** |

---

## 关键文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `requirements.txt` | +`djangorestframework` +`drf-spectacular` | DRF 核心 + OpenAPI |
| `config/settings.py` | +`REST_FRAMEWORK` + `SPECTACULAR_SETTINGS` | 全局配置 |
| `config/urls.py` | + schema + swagger 路由 | API 文档入口 |
| `shared/auth/drf_auth.py` | **新建** | JWT → DRF Authentication |
| `apps/evaluator/serializers.py` | **新建** | 4 个 ModelSerializer |
| `apps/evaluator/views_api.py` | **新建** | 2 个 ViewSet + `@action` |
| `apps/evaluator/urls.py` | **修改** | Router 替代手动 path |
| `apps/evaluator/views.py` | 删减 | 仅保留 kb_search / kb_self_test |

---

## 风险 & 缓解

| 风险 | 缓解 |
|------|------|
| 新旧 URL 路径变化导致前端 404 | 新旧端点**并行运行**一个迭代周期 |
| 响应格式 `{status, data}` 与 DRF 默认格式不同 | DRF `Response` 可自定义；或前端适配层统一处理 |
| 旧 views.py 直接 ORM 写 | ViewSet 中继续 ORM（`perform_create`），符合现有写操作铁律 |
| 后台线程不受 DRF 管理 | `@action` 内 `threading.Thread` 逻辑保持不变 |
| 当前 JWT 中间件与 DRF 认证冲突 | 新建 `drf_auth.py` 复用同一 `verify_token()`，不冲突 |
| KB 端点不是 CRUD | 保留为独立路径，与 Router 共存——展示 DRF 与非 DRF 可以混合 |

---

## 后续推广路径

试点成功后的顺序：

```
evaluator（当前试点）
    ↓
workflow（简单，10 端点，确认流程）
    ↓
case_manager（复杂，26 端点，4 个 case-type 用 ViewSet 多态）
    ↓
其他 App 按需渐进
```

**不迁移的**：device_pool（0% CRUD）、ai_assistant（SSE/WebSocket/async）、test_runner（异步执行器）
-------------------------

# 聊天图片上传与多模态发送 — 设计文档

## 1. 目标

## 2. 现状

## 3. 方案（已选：方案 1）

## 4. 交互设计

### 4.1 输入区

### 4.2 对话框

## 5. 数据流

## 6. API 契约

### 6.1 `POST /api/ai/upload-file`（扩展）

### 6.2 `POST /api/ai/conversations/{id}/chat/stream`（扩展）

### 6.3 历史消息 API

## 7. 后端实现要点

## 8. 前端实现要点

## 9. 错误处理

## 10. 验收标准

## 11. 测试建议

## 12. 风险与约束

---------------------
doodle-craft 做 UI + 维护主题（怎么做：令牌/组件/页面 + 自检）                             │
vue-frontend-check 工程门禁 + 风格校验（布局/契约/可达性/四态 + 圆角/阴影/间距/模块色/禁止事项）
DESIGN_SYSTEM.md   风格约束（硬编码值）+ 验收（分组标准）


Cascadia Code
Cascadia Mono