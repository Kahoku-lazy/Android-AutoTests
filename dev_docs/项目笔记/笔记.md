你这个计划有问题，只考虑到了执行，没有弄清楚目的，我看的也比较懵。
1. 每个修改点不能只关注改什么，要思考为什么这样改，修改的目的是什么，为了实现什么功能。
2. 要合理，且有逻辑的拆分任务。例如操作管理，要思考通用与独有的步骤如何管理怎么约束，Android用例编写时怎么输出操作步骤给用户编排，会不会Android中混入web步骤。
3. 需求与目录清晰了，在定位哪些文件要修改，主要修改哪些文件，哪些文件会有影响，要怎么修改
4. 修改完后再对齐目标与需求，验证自己的修改。 


## 编写用例

1. 阅读登录页面结构和元素

2. 查看现有测试用例格式和步骤类型

3. 查看 Web 元素定位器中已有的元素

4. 创建登录页面 Web 元素（如不存在）

5. 编写登录自动化测试用例并写入平台




编码规范要求
本项目所有 Python 代码须遵循以下规范：
代码风格统一遵循 PEP 8，行宽限制为 99 字符
命名规范：变量和函数使用 snake_case，类使用 PascalCase，常量使用 UPPER_SNAKE_CASE
所有公共函数和类必须编写 Docstring，说明参数、返回值和异常
提交代码前须通过 Black 格式化和 Ruff 检查，零 warning 方可提交
核心业务逻辑须附带单元测试，覆盖率不低于 80%
违反规范的代码将在 Code Review 阶段被退回。


`http://localhost:5173/runner/task/DIAG-MULTI-291222` 执行任务详情页怎么不见了


P1（6 项函数级重构）和 P2（5 项架构演进）是否继续？



做得好的：

1. 双通道自适应 — SSE 优先 + Django fallback 降级，容错健壮
2. ContentBlock 完整持久化 — blocks JSON 字段存储结构化内容，历史消息可完整还原 UI
3. 状态机分离 — SSEMessageBuilder 独立于 Vue，事件处理逻辑集中在 api.js
4. rAF 节流 — 处理高频 token 事件，避免渲染性能问题
5. ReAct 多轮支持 — _rounds[] 数组记录每轮思考+工具调用
6. 后台流保持 — detachStream 设计让用户切换对话时不影响 AI 继续生成
7. Credential 复用 — credential_hash 避免每次创建 session 都重建 AgentScope credential

可改进的点：

1. fallbackSend 端点似乎不存在 — useSSE.js 调用了 /ai/conversations/{id}/send，但在 urls.py 中没有注册这个路由。这意味着降级模式实际上不工作，会导致 fallback 静默失败。
2. watchdog 45s 硬编码 — 对于慢模型或复杂工具调用可能不够，应该有重试或用户可配置的超时。
3. AIMessage.blocks 存储效率 — 完整的 ContentBlock 数组（含 thinking 的完整文本）作为 JSON 文本字段存储，对于长对话可能很大。考虑对 thinking 块做截断或压缩。
4. 前端 ChatView.vue 438 行 <script> — 接近前端规则中的 400 行上限。_updateTaskCardProgress 和 task 相关的逻辑可以考虑抽到 composable。
5. AgentScope session 无超时清理 — AIConversation.agent_scope_session_id 指向的 AgentScope session 创建后没有主动清理机制，依赖 AgentScope 的 workspace TTL（1 小时）。
6. SSE 重连逻辑缺失 — 当前代码注释提到"自动重试最多 3 次"，但 subscribeStream 中并没有实现重连。断线直接走 onError → fallback。

  Thought for 1s