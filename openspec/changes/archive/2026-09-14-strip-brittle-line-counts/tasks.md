## 1. 事实与范围

- [x] 1.1 枚举 `frontend/AGENTS.md` 内所有 `\d+ 行` 表达；验证：14 处命中中 **11 处为文件规模**（L0 §② ×4：`全文 16 行` / `全文 37 行` / `**33–55 行**（全文约 310 行）` / `**18 行起**（全文 428 行）`；L1 §② ×7：`全文 71 行` / `302 行` / `~520 行` / `~55 行` / `116 行` ×2 / `75 行`），3 处为正文（`一屏 4 个一排` · `超过 500 行需拆分` · L2 导语），全部保留
- [x] 1.2 复核 L1 §③ 行号断言；验证：`tokens.css:213` 指向 `--side-w`、`:212` 指向 `--app-topbar-h`（回读确认），即上一轮刚修过的漂移点

## 2. 改写

- [x] 2.1 L0 §② 表 4 行「位置」列改为性质描述；验证：`唯一 HTML 外壳` · `入口脚本（无组件）` · `**顶部全局段**（其后为组件类）` · `**开头 `:root` 块**（末尾另有全局工具类）`，4 行内无 `\d+ 行` 与行区间
- [x] 2.2 L1 §② 表 7 行「位置」列改为 `整份` 系列；验证：7 行分别为 `整份` ×5 · `整份（scoped 引入）` · `整份（纯常量）`，无行数
- [x] 2.3 L1 §③ 去掉行号；验证：改为 `--side-w`（`tokens.css 根级`）· `--app-topbar-h`（`tokens.css 根级`，…），全文 `tokens\.css:\d+` 命中 **0**

## 3. 验证

- [x] 3.1 复核全文；验证：`\d+ 行` 类命中由 11 处文件规模降为 **0 处**（仅剩 `超过 500 行` 规则阈值与正文表达——见 1.1 的保留项）
- [x] 3.2 改动范围；验证：`git diff --numstat -- frontend/AGENTS.md` = `294 16`（本变更），未触及任何源码文件
