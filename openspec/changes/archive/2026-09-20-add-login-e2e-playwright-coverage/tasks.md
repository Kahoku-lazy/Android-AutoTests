## 1. 夹具与选择器

- [x] 1.1 `tests/e2e/selectors.py` 补齐本变更用到的 testid 常量（注册卡 6 个、侧栏登出 1 个）并分组注释；验证：`python -m pytest tests/e2e -q --collect-only` 无 ImportError。
- [x] 1.2 `tests/e2e/conftest.py` 新增 `e2e_account`（session 级、自建 `e2e_probe`、已存在则登录复用、失败即 fail）与 `login_page`（打开 `/login` 并等 `login-page` 就绪）夹具；验证：`python -m pytest tests/e2e -q -k E2E_001` 通过且不带任何环境变量。
- [x] 1.3 `tests/e2e/conftest.py` 新增登录请求计数 helper 与 `page.route` 延迟放行 helper；验证：E2E-005 与 E2E-007 能把计数与 loading 断言跑绿。
- [x] 1.4 删除占位示例 `tests/e2e/test_example_e2e.py`，确认 `grep -r "admin_creds" tests/e2e` 只剩 conftest 里保留的兼容夹具（若有）；验证：`python -m pytest tests/e2e -q` 收集到的用例全部不依赖 `TEST_ADMIN_PASSWORD`。

## 2. 登录主链路与错误路径（E2E-001 / 002 / 011 / 012）

- [x] 2.1 E2E-001 登录成功进工作台且恰好 1 次登录请求；验证：该用例通过，且故意放宽计数为 "> =1" 时断言会失败（自检断言有效性）。
- [x] 2.2 E2E-002 失败 → 覆盖层文案 → 「知道了」关闭 → 输入保留 → 改后重试成功；验证：用例通过，中途断言过「输入框值仍为原值」。
- [x] 2.3 E2E-011 注册即登录进工作台（用带时间戳的唯一账号，避免与历史数据相撞）；验证：用例通过且断言 URL 落到 `/dashboard`。
- [x] 2.4 E2E-012 侧栏登出回 `/login`；验证：用例通过且断言登出后 localStorage 账号池不再含该账号。

## 3. 按钮态与误操作（E2E-003 / 004 / 005 / 006 / 007 / 008）

- [x] 3.1 E2E-003 空表单按钮禁用 + 强制点击 0 请求；验证：用例通过（`force=True` 点击后计数为 0）。
- [x] 3.2 E2E-004 只填一项仍灰 → 填齐点亮 → 151 字符回到灰且给出「用户名过长，最多150个字符」；验证：用例通过。
- [x] 3.3 E2E-005 1 秒内连点 5 次 → 1 次请求 + 1 层覆盖层；验证：用例通过。
- [x] 3.4 E2E-006 长按 1.2 秒 → 1 次请求；验证：用例通过。
- [x] 3.5 E2E-007 延迟放行下 loading 且不可再点，放行后进工作台；验证：用例通过。
- [x] 3.6 E2E-008 回车不提交（当前行为，注释标注待决项 1）；验证：用例通过，且注释里给出「产品若支持回车提交需改断言」的指引。

## 4. 记忆、模式与可达性（E2E-009 / 010 / 013 / 014 / 015）

- [x] 4.1 E2E-009 记住账号往返（`/login?add` 预填 + 开关为开；取消后 `saved_username` 被清）；验证：用例通过并读取 `localStorage.saved_username` 两级断言。
- [x] 4.2 E2E-010 模式切换（CTA `aria-pressed` 与便签标题同步）；验证：用例通过。
- [x] 4.3 E2E-013 / 014 路由守卫两条（未登录访问受保护页回登录；已登录访问 `/login` 去工作台）；验证：两条用例通过。
- [x] 4.4 E2E-015 窄屏 480×844 无横向溢出且登录卡可达；验证：用例通过（断言 `scrollWidth <= clientWidth + 1`）。

## 5. 全层验证与文档同步

- [x] 5.1 全量端到端层跑绿：`python -m pytest tests/e2e -q`（前后端需在跑）；验证：15 条全 passed，无 skipped。
- [x] 5.2 连跑两次确认无残留副作用与顺序依赖：第二次仍然全 passed（账号复用路径被覆盖）；验证：两次输出的 passed 数一致。
- [x] 5.3 文档同步 `dev_docs/DEV_TEST/功能测试用例/功能测试用例-登录.md`：第四节端到端层现状由「当前为空」改为「15 条已落地」并列出编号映射，第五节缺口第 3 条同步；验证：`grep -n "端到端" 该文件` 无「当前为空」表述。
- [x] 5.4 文档同步 `dev_docs/ARCH_PRD/PRD-00-登录模块.md` 测试章：会话节的「端到端用例尚未落地」按实际落地范围改写（TC-SESSION-001/002/007 已落地，其余仍待），附录第 2 条同步；验证：PRD 中端到端相关表述与 `tests/e2e` 实际用例数一致。
- [x] 5.5 运行方式写进文档并实测：把 `python -m pytest tests/e2e -q` 与「无需密钥」写进两处文档；验证：按文档命令在干净 shell 下执行成功。
- [x] 5.6 归档前跑一次项目门禁中与本变更相关的部分（`python -m pytest tests/graybox/unit -q`，确认没有连带破坏）；验证：单元层结果与变更前一致。
