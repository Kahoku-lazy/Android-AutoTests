## 1. 设备选择与分页

- [x] 1.1 核对设备选择触发键与下拉浮层为白底、无阴影（悬停与聚焦也不出现偏移阴影，墨线边框保留），并确认工作区已有覆盖与此一致；验证：在 `/inspector` 查看触发键与展开浮层的计算样式，背景为白色且 `box-shadow` 为 `none`
- [x] 1.2 核对元素表每页固定 7 行，常量仍只登记在模块常量中；验证：筛选结果超过 7 行时首屏只渲染 7 行，分页文案为「第 1 / N 页」，分页区没有「显示行数」

## 2. 空表高度

- [x] 2.1 核对无数据时表纸锁在结构栏高度内，空态文案可见；验证：未获取快照时表纸底边在窗口内，「未选中设备」出现在表纸里，刷新后高度不再持续变大

## 3. 双击标识高亮

- [x] 3.1 去掉表格行单击对选中态的写入，单击「元素名称」只进入重命名、不再写入选中态；验证：单击「元素名称」出现重命名输入框，手机画面红框不变
- [x] 3.2 双击「标识」才写入选中元素，并在手机截图上按该元素坐标画红色区域（同一时间只有一个红框；宽或高为 0 不画）；验证：单击「标识」不出现新红框，双击后红框与该行 bounds 重合，再双击另一行则红框移到新坐标
