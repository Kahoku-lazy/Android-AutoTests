## 1. 选择器版式

- [x] 1.1 把弹窗标题改成「已保存页面」，并单独标出「只读」；打开弹窗确认标题不再是「打开元素定位已保存页面（只读）」
- [x] 1.2 页面节点改成两行（名称一行，包名与元素数一行），目录节点加粗且没有第二行；用现有已保存页面打开弹窗，确认「测试页面」这类无包名项第二行是「—」而不是跟名称连在一行
- [x] 1.3 点一个页面仍只读打开并关闭弹窗；点目录只展开或收起、检查器当前页面不变。样式只用已有令牌，本文件新增样式里不出现写死的颜色值
- [x] 1.4 有目录时，目录下的页面相对该目录向右缩进，根级页面与目录左缘对齐；若当前数据没有目录，在对比页或浏览器里用树的 indent 确认子节点未被拉齐

## 2. 回归

- [x] 2.1 空列表仍显示「暂无已保存页面」。若当前环境没有空数据，用组件里的空态文案对照确认，并在浏览器里走通有数据的打开路径
