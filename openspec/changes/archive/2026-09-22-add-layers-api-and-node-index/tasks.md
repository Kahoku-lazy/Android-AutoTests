## 1. 采集端：全量节点索引落库

- [x] 1.1 在快照模型新增「全量节点索引」字段（JSON，默认空列表）并生成迁移，验证：`python manage.py makemigrations --check` 干净、`python manage.py check` 通过
- [x] 1.2 采集编排把**未裁剪**节点写入索引（含坐标、类名、资源标识、文本、内容描述、父内序号、层级、七项交互标志、保留标记；不含 XPath 候选），验证：新增单测断言「索引节点数 = 解析节点数」且「保留标记为真的条数 = 展示裁剪后条数」
- [x] 1.3 确认索引随快照删除而消失、且不影响既有快照详情与列表，验证：`pytest tests/graybox -q` 既有用例全绿

## 2. 分层查询端点

- [x] 2.1 服务层实现「索引 → 两级分组摘要 + 元素条目」（调用算法层判据，先筛减、后算主定位），验证：单测断言一级四组 + 内容控件二级三组的计数与总量一致，且「其它」计数为 0 时仍出现
- [x] 2.2 服务层实现筛减与分页（一级分组、二级分组、交互标志多选、关键词、仅保留、仅稳定、偏移与条数），验证：单测覆盖「按二级分组筛减」「标志 + 关键词组合」「分页区间与命中总数一致」三类
- [x] 2.3 新增视图与路由 `GET /api/inspector/snapshots/{id}/layers/`，解析筛减参数并对未知分组/非法分页返回 400、对不存在或非本人快照返回 404，验证：接口测试通过且响应遵循 `{status,data}` 信封
- [x] 2.4 历史快照降级：索引为空时按保留集返回并标注数据来源与「不含被裁元素」，验证：单测断言降级响应无「保留标记为假」的条目且带来源标记
- [x] 2.5 归属过滤：层查询只返回当前调用者自己的快照，验证：单测断言他人快照返回 404

## 3. 响应瘦身：只回主定位

- [x] 3.1 分层响应中的元素条目只带一条主定位（表达式/类型/匹配数/是否稳定），MUST NOT 携带候选全量；候选列表不进响应体，验证：接口用例断言响应条目中不含候选数组字段，且同一页（140 元素量级）响应体不超过 100 KB（实测 93.2 KB；对照：带候选全量为 134.6 KB）

## 4. 文档与门禁

- [x] 4.1 文档面同步：端点带 `@extend_schema`，由官方文档面（`/api/schema/` + `/api/swagger/`）自动收录；历史手写文档 `API-设备检查器.md` 同步为 8 端点并补「快照分层查询」小节与 `nodes_json` 字段说明。验证：`python manage.py spectacular --file -` 命中 `inspector_snapshots_layers_retrieve`；手写文档第 1 节总览表行数与 `urls.py` 的 path 条数一致（8）
- [x] 4.2 落单门禁全量执行：`python manage.py check`、`ruff check`、`python manage.py makemigrations --check`、`pytest tests/graybox -q`、`python tools/gen_arch_stats.py --check-boundaries`，验证：全部通过