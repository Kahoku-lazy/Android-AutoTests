# ── Security ──
cryptography>=41.0

# ── Django Core ──
Django>=4.2
djangorestframework>=3.15
drf-spectacular>=0.28
daphne>=4.0
channels>=4.0
channels-redis>=4.0
django-cors-headers>=4.0
django-jazzmin>=3.0

# ── Database ──
mysqlclient>=2.2
redis>=5.0

# ── Device Control ──
uiautomator2>=3.0
opencv-python-headless>=4.5
Pillow>=10.0

# ── OCR ──
cnocr>=2.3.1,<3.0

# ── HTTP ──
requests>=2.30

# ── Testing ──
pytest>=8.0
pytest-django>=4.0
allure-pytest>=2.13
jsonschema>=4.0
pytest-playwright>=0.5.0

# ── AgentScope 2.0 AI ──
# ≥2.0.7.post1 才包含 pipeline 模块（GoalPipeline）；main 分支当前版本号仍为 2.0.7.post1
agentscope>=2.0.7.post1
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pyjwt>=2.8.0
chromadb>=0.4.0

# ── AI Agent Evaluation Frameworks ──
evalscope>=1.0.0
deepeval>=3.0.0
maseval>=0.1.0
