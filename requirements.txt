# ── Security ──
cryptography>=41.0

# ── Django Core ──
Django>=4.2
daphne>=4.0
channels>=4.0
channels-redis>=4.0
django-cors-headers>=4.0
django-jazzmin>=3.0

# ── Database ──
mysqlclient>=2.2
redis>=5.0

# ── Device Control ──
uiautomator2>=3.0
Pillow>=10.0

# ── HTTP ──
requests>=2.30

# ── AgentScope 2.0 AI ──
agentscope>=2.0.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pyjwt>=2.8.0
chromadb>=0.4.0
